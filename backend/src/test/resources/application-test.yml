spring:
  datasource:
    url: jdbc:h2:mem:kaoyan_buddy;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  ai:
    model:
      chat: none
    deepseek:
      api-key:

app:
  cors:
    allowed-origins: http://localhost:5173
  jwt:
    secret: test-kaoyan-buddy-secret-value-with-at-least-32-bytes
    expiration-minutes: 60
